{
  "name": "@cie/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Client Intelligence Ecosystem — the Client Intelligence Engine: canonical client store, ingest+reconcile engine, and the single service apps read client truth through.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "tsx src/__checks__/run.ts"
  },
  "dependencies": {
    "@cie/contracts": "*",
    "zod": "^4.1.5"
  },
  "peerDependencies": {
    "firebase-admin": "^13.0.0"
  },
  "peerDependenciesMeta": {
    "firebase-admin": { "optional": true }
  },
  "devDependencies": {
    "firebase-admin": "^13.5.0",
    "tsx": "^4.20.5",
    "typescript": "^5"
  }
}
